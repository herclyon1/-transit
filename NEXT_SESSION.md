# 下一会话交接单

> **2026-08-07 追加（quiz v27 大修 + 仓库清扫）**
> - quiz/japan-map-quiz.html 从 3.55MB 瘦到 294KB：ADMIN1 外置为 quiz/admin1.topojson（只有大日本模式懒加载）。
> - 東亜地图改 Canvas 渲染（`#eaCv`），几何预烘焙 Path2D；单元不再是 DOM 节点，点击走 eaHitTest。
>   数据为 geoBoundaries CGAZ 三档 LOD：quiz/ea_t0|t1|t2.topojson（1500m/300m/80m），
>   内含 units / disp / water / rivers 四个图层。**分档必须全局统一，不可按单元混档**（会出缝）。
> - 性能基线（4x 降速手机模拟 390x844）：首屏 < 2.1s、k=30 拖动帧 p50 ≈ 25ms。
>   复测脚本思路见 quiz 文件头注释；超过 33ms 视为回归。
> - 已删除死文件约 91MB：admin/archive-japan_admin_map_mobile.html（被拒绝的阉割手机版）、
>   quiz/archive/ 十个旧版、大阪车站线路叠加图_v0.html。全部可从 git 历史取回。
> - transit / admin / osaka-residence-tiers 三页已逐页浏览器验收：零 JS 报错、零 404。


把这份文件 + `HANDOFF.md` 一起给下个会话读。分支 `claude/handoff-review-plan-dvfspv`，
所有产出已推送到 GitHub，Pages 已开（`https://herclyon1.github.io/-transit/`）。

---

## 一句话现状

v0（显示层）→ v1（时间层 + 四枢纽 Dijkstra）→ 渲染层 → **实路网步行等时圈**
全部完成并实测验证。上一份交接单的待办 A / B / C 全部关闭（2026-08-06 会话）。

---

## 本会话完成的三件事（含证据）

### 待办 A（关闭）：高倍放大面覆盖边缘发糊

格子粗细不再只看步行半径，而是先数视野内有效站数，按
「スタンプ更新数 + argmin走査数 ≤ 2.5M」反解最细格子，下限放宽到 1 设备px。
单站涂色半径 实测/期待比值：z11 = 1.047、z13 = 1.012、z15 = 1.003、z16 = 1.002
（改前高倍劣化到 1.2 级）。最重场景（z11、B=20、全站）绘制 87.7ms。

### 待办 B（关闭）：缩小时点挤成一团

三管齐下：衰减 0.80→0.72 / 下限 0.34→0.22；描边宽度同步随缩放收缩
（2.2px 白边在 z9 比点本体还宽，是团子主因之一）；zoom≤10 时同屏幕格子
（z10 用 6px、z9 用 8px）按类别只画一个代表站——可达枢纽多者优先、其次最快者。
去重只影响格内多站的地方，疏地区原样。统计数字在去重前算好，不受影响。
重叠对实测：z9 2168→77（-96%），z10 777→255（-67%）。
点击判定圈下限 9px 没动（独立参数）。

### 待办 C（关闭）：实际步行路网等时圈

**没有自建路网**。上份交接单按「Geofabrik pbf + pyrosm 自建行人图」估了 1–2 个
完整会话，实际用 FOSSGIS 公共 Valhalla 等时圈接口一步到位（约半个会话）：

- `fetch_walk_polys.py`：842 站 × 5/10/15/20 分四带，步速 4.8km/h 与显示层一致，
  1.2s/req 限速 + `walk_cache/` 断点续传（已 gitignore），汇成 `walk_polys.json`
- 前端 `AreaLayer`：帯多边形扫描线填充（特意不用 canvas 填色——抗锯齿会把
  帯号混出错档）。步行分 = max(直线分, 帯下限)，仍是真值下界；
  滑块恰为 5/10/15/20 时边界与路网等时圈**严格一致**，中间值用直线分带内插值。
  帯下限是开区间下界：lo ≥ B 的帯在 B 分内不可达，直接跳过（别改成 ≤，会多塗）
- `walk_polys.json` 懒加载（勾面覆盖才 fetch）；数据未到/个别站缺失时退回直线圆，
  缺失数显示在面板 `#areastat`
- 验证（独立实现交叉验证：页面走査线 vs Path2D.isPointInPath）：
  桜ノ宮/天満橋 × B=7/10/15/20 → IoU 0.983–0.996，面积比 1.000–1.005。
  靠河站真实步行面积 = 直线圆盘的 52–66%，印证当初「7–8 成、靠河更狠」的估计

---

## 本会话第二轮（同日追加）

1. **直通運転修正**（build_reach.py `THROUGH`）：OSM relation 按事业者切断导致的
   假换乘罚时——長田（中央線⇄けいはんな線）、天六（堺筋線⇄阪急千里線）、
   中百舌鳥（高野線⇄泉北線）三处直通边罚时归零。荒本→梅田 29.6→24.6、
   →京橋 23.3→18.3。標尺 6/6 通过。御堂筋線⇄北大阪急行在 OSM 里本来就是一条
   pattern 不用修；阪神なんば線⇄近鉄奈良線的大阪難波在難波枢纽群内，不影响枢纽时间
2. **帯内插值换迂回系数**（prepWalkK）：k=帯轮廓顶点直线分均值反解的遠回り度，
   徒歩分=clamp(直线分×k, 帯下限, 帯上限)。滑块逐分增幅从带边界暴增
   变为 0.010–0.040 平缓带；整档 5/10/15/20 的边界一致性保持不变
3. **新 UI**：可达数量筛选 ≥1/2/3/4（546→297→201→138 站单调）；
   势力范围模式（隐藏点、alpha 235、异枢纽相邻格画深色国境线）；
   枢纽星标改为缩放联动菱形+标签芯片（z9 只剩 6px、标签收起）、可开关；
   默认面 alpha 140→175
4. 验证脚本 poly.js 期待模型已同步新插值语义（k 独立重算）。
   IoU 0.97–0.99、面积比 1.000–1.006 全通过
5. **点地图任意处查可达性**（pointQuery）：非车站位置弹「此地点→四枢纽」
   四元组＋经由站＋步行分（同一套等时圈帯+迂回系数，1700m 粗筛→PIP 精判）。
   居住决策的本命用法。注意 Leaflet 是 `.lng` 不是 `.lon`（已踩过）
6. **第三轮（用户反馈后）**：主题三态 自动🌗(跟随 prefers-color-scheme)→暗→浅，
   自动态实时跟随系统切换，theme-color meta 同步地址栏；浅色 accent 从
   #a37f00（屎黄）换 #b45309 琥珀（白底 4.5:1），--accfg 配套；
   任意点查询改**长按/右键**触发（contextmenu，Leaflet 长按自动映射）+
   红图钉，popup 关闭图钉即收；**in_v0 错标修正**——阪堺全线曾因 v0 清单
   缺停留所被标「府外」，reach.json 改用 japan.geojson 大阪府多边形 PIP
   生成 in_osaka 字段（34 站纠正，府内 450→484），前端已跟进

## 精度升级（86% borrowed 的解法，等用户给 key 后做）

prov 是「路径上最差出典」，所以只要沾一段 borrowed 整站就算 borrowed——86% 是
这么来的。要把绝大多数站变成实测级，只有拿真值：**842站×4枢纽=3368 组 OD 查询**，
取平日日中出发的最短所要时间，一次性抓完缓存进 repo，之后不再依赖 API。

- ~~Google Routes API~~ **已证伪**（见死路清单）
- ~~NAVITIME~~ 法人向け纯商谈，个人拿不到（用户调研 2026-08）
- ~~Ekispert フリープラン~~ 経路探索只返回 for-Web URL，无结构化数据
- **首选 Ekispert 90日無料評価版**（标准计划全功能试用，用户申请中），
  备胎 **買い切り型请求包**（数千円级，一次性需求正对路）
- **口径**（已定稿，勿重开）：用逐段时刻算「末段下车时刻−首段上车时刻」，
  剔除出发等待与两端步行、保留换乘衔接；上车站名与预期不符/含收费特急的样本打标
- **抽样校准设计**（省 API 量的关键）：不暴力查 3368 次。按下表抽样框，
  每条 线×种别 抽 2-4 个代表 OD（约 300-600 次）→ 反解该线巡航速度
  （anchors 机制现成，line_times.py ANCHORS 格式）→ prov 升 anchored/api
  → 再抽小批验证。只有校准不收敛的线才逐站扫。
  抽样框（缺锚 线×种别 按途经站次 top，府内相关）：
  近鉄大阪線普通96 / 南海高野線普通84 / 南海本線普通82 / JR紀州路快速78 /
  JR片町線快速72 / JR宝塚線快速70 / 阪神本線普通66 / 南海空港線普通60 /
  JR関空快速58 / 近鉄南大阪線普通56 / 近鉄京都線普通52 / JR東海道本線普通265(北部)
  完整排名跑 line_times.solve_speeds 按 borrowed 过滤即得
- 手工锚点补测已试过放弃：kansai-norikae 是博客体不成体系，
  Yahoo 时刻表页要两站配对成本高——等 API 数据一次到位更省
- 抓回来后：t_default 直接换成 API 值（保留模型值做 diff 报告），prov 加
  `api` 等级，⚡/💴 语义按 API 返回的种别重标。工作量约半个会话
- 顺带能校准：JR宝塚線・環状線快速这些缺优等停站型的线（现在偏高估）

## 项目整合（用户已确认方向）

用户另有三个项目并入本仓库为子页面：Pages 子目录 /grade/（大阪居住等级图,
治安×环境 S-D 分区 —— 即 HANDOFF 项目定义里的「面层」!）、/admin/（日本行政
区分図）、/quiz/（都道府県地图 quiz, 4MB 单文件, v20-23 有版本考古待修,
建议单开会话）。居住等级图除共存外要做成可达图里的叠加图层（双层结构补完）。
等用户上传 HTML。

## 剩余方向（按 HANDOFF.md 的路线图）

- **v2**：电助力自行车接驳层（~12km/h 有效速度扩大车站势力圈）。
  同一个 Valhalla 接口把 costing 换 `bicycle` 即可，脚本现成
- **v3**：bus 补丁（東大阪腹地/大東方向轨道空白区）
- 小项：优等停站型仍有 41/80 条线缺（JR宝塚線・環状線快速影响最大，时间偏高估）；
  阪堺停留所缺失；阪急淡路（千里線⇄京都本線 同社直通）未入 THROUGH，影响约3分

---

## 怎么验证（每次改完都要跑）

容器里有 Playwright，浏览器在 `/opt/pw-browsers/chromium`，**不要跑
`playwright install`**。沙箱里 CDN 连不通：把仓库文件拷到临时目录，
Leaflet 换本地文件，`python3 -m http.server` 起服务。

本会话的现成脚本（在会话 scratchpad `test/` 下，思路可复用）：
- `run.js`：单站涂色半径 实测/期待（应 1.0–1.05）+ 绘制耗时 + 浅色/窄屏/pageerror
- `overlap.js`：新旧 zoomScale 的重叠对数对比 + 截图
- `poly.js`：等时圈渲染 IoU（应 ≥0.98）；期待形状用 Path2D 独立算，别抄页面实现

**其他必测项**：明暗主题各测一遍；412px 窄屏面板不遮滑块；`pageerror` 必须为空。

---

## 已经确认过、别再重走的死路

- **Google Routes API 查日本公共交通**：computeRoutes TRANSIT 对日本全境返回
  空对象（新宿→東京駅都为空；同 key 伦敦正常，2026-08 实测）。日本换乘数据
  只授权消费者版 Google Maps，API 不开放。fetch_truth.py 的骨架保留，
  换 provider 时改 query() 即可
- **GTFS**：gtfs-data.jp 全量 547 feed，大阪府只有 1 条高速巴士。2026-08 实测
- **Overpass 拉全府步行路网**：必被限流。等时圈用 Valhalla 接口，路网都不用碰
- **自建行人图（pbf+pyrosm）**：没必要，公共 Valhalla 等时圈接口
  （`valhalla1.openstreetmap.de/isochrone`）免费无 key，1 req/1.2s 礼貌限速，
  842 站 ~20 分钟抓完。costing_options 里记得设 walking_speed=4.8
- **Wikipedia wikitext 直读駅一覧表**：格式差异过大，必须渲染后 HTML
  （`fetch_express.py` 已实现）
- **Pages API**：代理禁 `/repos/*/pages`，只能手动在 Settings → Pages 改
- **canvas 填色做帯栅格化**：抗锯齿会在帯边界混出假档位，用扫描线

## 历史坑清单（都已修）

| 坑 | 症状 | 根因 |
|---|---|---|
| 混色配色 | 三枢纽可达的站显示成灰点 | 红+蓝+绿平均 = 正灰 |
| popup 浅色不可见 | 阈值内分钟数空白 | 硬编码 `color:#fff` 撞白底 |
| 等待罚时 | ⚡ 图层永远不生效 | 罚时加在每条边而非上车一次 |
| 速达合算 | 南海高野線整条掉 B 层 | pattern 求和把上下行重复计入 |
| 格子按 CSS px | 边缘发糊 | 高 DPR 下 1 格 = 6–9 设备像素 |
| 格子只看半径 | 高倍边缘发糊 | 放大后站少，有余力用细格子（待办 A） |
| B=10 期待模型 | 验证时 IoU 掉到 0.54 | 帯下限是开区间下界，lo≥B 应排除（是测试错，不是页面错） |
