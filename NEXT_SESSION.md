# 下一会话交接单

把这份文件 + `HANDOFF.md` 一起给下个会话读。分支 `claude/handoff-review-plan-dvfspv`，
所有产出已推送到 GitHub，Pages 已开（`https://herclyon1.github.io/-transit/`）。

---

## 一句话现状

v0（显示层）→ v1（时间层 + 四枢纽 Dijkstra）→ 渲染层（阈值滑块 / 低频优等图层 /
明暗主题 / 面覆盖）全部跑通并上线。剩下的是**渲染精细度**和**步行模型真实度**两件事。

---

## 待办 A：高倍放大时面覆盖边缘发糊（小改，10 分钟级）

**这跟"直线步行 vs 实际路网"无关，是纯渲染参数问题。别把两件事混在一起。**

`index.html` 的 `AreaLayer.draw()` 里：

```js
const CELLdev = Math.max(2, Math.min(6, Math.ceil(RdevRaw/90)));
ctx.filter = CELLdev > 2 ? 'blur('+(CELLdev*0.35).toFixed(1)+'px)' : 'none';
```

放大后 `RdevRaw`（步行半径的设备像素数）变大 → `CELLdev` 被推到上限 6 →
1 个格子 = 6 个设备像素，再叠 2.1px 模糊 → 边缘就是几像素宽的渐变带。

**问题出在 `CELLdev` 只看半径，没看"屏幕上到底有几个站"。** 放大后视野里可能只剩
10–30 个站，完全有余力用更细的格子。正确做法是按**总计算量预算**定格子：

```
可见站数 × min(圆面积, 视口格子数) ≤ 预算（比如 300 万 cell-update）
```

反解出 CELLdev，而不是拿半径除以固定的 90。低倍时站多 → 格子粗一点；
高倍时站少 → 格子细到 2，边缘自然锐利。

改完必须复测（脚本见下方「怎么验证」）：单站涂色半径的**实测/期待比值**要保持在
1.0–1.05（当前低倍是 1.03，高倍因为格子粗会劣化）。

---

## 待办 B：缩小时点仍然挤在一起

当前：

```js
Math.max(0.34, Math.min(1.0, Math.pow(0.80, BASE_ZOOM - map.getZoom())))
```

zoom 9 时系数 0.64、下限 0.34，最大可视半径约 3.5px。用户实测仍嫌挤。

可选做法（建议按顺序试）：
1. 把衰减底数从 0.80 调到 0.72、下限降到 0.22 —— 一个常数的事，先试这个
2. 低倍（zoom ≤ 10）时只画「四枢纽全覆盖」和「仅低频线可达」两类，其余隐藏
3. 低倍时做像素级去重：同一屏幕格子内只保留最快的那个站

**注意**：点半径变小后要保证 `hit()` 的透明点击圆下限（当前 9px）不跟着缩，
否则手机上点不中。这两个是独立参数，别一起调。

---

## 待办 C：实际步行路网等时圈（大工程，建议单开一个会话专做）

### 先说清楚它能解决什么、不能解决什么

- **不能**解决"边缘发糊"——那是待办 A，是渲染问题
- **能**解决的是**形状失真**：现在是直线圆盘，实际有大川・淀川・阪神高速・
  铁路本身这些屏障。真实可步行面积大约只有圆盘的 7–8 成，靠河的车站高估更明显

### 工作量评估：约 1–2 个完整会话

| 步骤 | 内容 | 风险 |
|---|---|---|
| 1 | 取関西的 `.osm.pbf` 抽出（Geofabrik，约 300MB）。**不要用 Overpass** 拉全府步行路网，量太大必被限流 | 下载耗时、磁盘 |
| 2 | 用 `pyrosm` 或 `osmium` 抽 `highway=footway/path/pedestrian/steps/residential/living_street/service/unclassified/tertiary/secondary/primary/track`，建行人图（百万级节点） | 内存，可能要分块 |
| 3 | 842 个车站做多源 Dijkstra，截断在 20 分（1.6km） | 计算量可控 |
| 4 | 每站导出 4 个步行等值线多边形（5/10/15/20 分），alpha shape 或格子轮廓 | 多边形化是最容易踩坑的一步 |
| 5 | 体积控制：842 站 × 4 级 × 约 50 点 ≈ 2–4MB JSON，gzip 后约 1MB。超了就降到 3 级或简化多边形 | |
| 6 | 前端把 `AreaLayer` 的「圆」换成「多边形栅格化」，argmin 逻辑完全不用改 | |

**关键设计不变**：每个格子仍然是对四枢纽各算一遍
`min over 车站(轨道时间 + 步行时间)`，取最快的那个上色。只是"步行时间"从
直线距离改成路网距离。

**中间方案**（如果嫌重）：只对**河流/高速这类大屏障**做惩罚——把跨越屏障的直线
路径按 1.5–2 倍计权。工作量小一个数量级，能吃掉大部分失真。可以先做这个看效果。

---

## 怎么验证（每次改完都要跑）

容器里有 Playwright，浏览器在 `/opt/pw-browsers/chromium`，**不要跑
`playwright install`**。测试脚本模式（本会话用过的，可直接复用）：

```bash
# 1. 把仓库文件复制到临时目录，把 CDN 的 Leaflet 换成本地文件
#    （沙箱里 cdnjs 连不通，直接开页面会报 "L is not defined"）
# 2. cd 到那个目录 setsid python3 -m http.server 8899 &
# 3. node 脚本用 chromium.launch({executablePath:'/opt/pw-browsers/chromium'})
```

**面覆盖的精度回归测试**（本会话抓出 1.20 倍误差就是靠它）：
把 `DATA.stations` 临时替换成单个车站 → 画一帧 → 扫 canvas alpha>30 的最远像素
→ 和 `(步行分 × 80m) / (米/像素)` 比。比值应在 1.0–1.05。

**其他必测项**：明暗主题各测一遍（浅色下曾出现过 popup 白字白底看不见）；
手机窄屏 412px 视口测面板不遮挡滑块；`pageerror` 必须为空。

---

## 怎么开下一个会话（手机端）

1. Claude App → 左侧 **Code** 标签 → 新建会话，选仓库 `herclyon1/-transit`
2. 分支填 `claude/handoff-review-plan-dvfspv`（**不要开新分支**，否则 Pages 指向的
   分支就不是你在改的那个了）
3. 第一句话就贴：

> 读 NEXT_SESSION.md 和 HANDOFF.md，然后做待办 A 和 B。
> 评价标准照旧：证据先行、精度诚实标注、不做拍脑袋加权。
> 改完用 Playwright 实测再报给我，别只说"应该好了"。

待办 C 建议**再单开一个会话**，第一句贴：

> 读 NEXT_SESSION.md 的待办 C，做实际步行路网等时圈。
> 先只做第 1–2 步（拿 pbf、建行人图）并报数据量给我，我确认后再往下。

---

## 已经确认过、别再重走的死路

- **GTFS**：gtfs-data.jp 全量 547 feed，大阪府只有 1 条高速巴士。Metro 和关西各社
  全部没有。2026-08 实测
- **Overpass 拉全府步行路网**：必被限流，用 pbf 抽出
- **Wikipedia wikitext 直读駅一覧表**：各条目格式差异过大会破产，
  必须用渲染后 HTML + 展开 rowspan/colspan（`fetch_express.py` 已实现）
- **Pages API**：本会话的代理禁止访问 `/repos/*/pages` 端点，只能你手动在
  Settings → Pages 里改

---

## 本会话踩过的坑（都已修，写下来防复发）

| 坑 | 症状 | 根因 |
|---|---|---|
| 混色配色 | 三枢纽可达的站显示成灰点 | 红+蓝+绿平均 = `rgb(133,134,148)` 正灰 |
| popup 浅色不可见 | 阈值内的分钟数空白 | 硬编码 `color:#fff` 撞白底 |
| 面板遮挡 | 宽屏下滑块点不到 | `#info` 与 `#bar` 同为 `left:8/top:72` |
| 等待罚时 | ⚡ 图层永远不生效 | 罚时加在每条边上，坐 3 站罚 3 次 |
| 速达合算 | 南海高野線整条掉进 B 层 | 按 pattern 求和会把上下行重复计入 |
| 格子按 CSS px | 边缘发糊 | 高 DPR 下 1 格 = 6–9 设备像素 |
